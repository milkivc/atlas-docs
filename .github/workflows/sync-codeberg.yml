name: Legacy Codeberg sync (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This duplicate workflow is intentionally disabled."
          echo "Use .github/workflows/mirror-to-codeberg.yml."
          echo "Destructive git push --mirror --force is no longer permitted."
