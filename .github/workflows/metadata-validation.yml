name: Metadata Validation

on:
  push:
    branches: [hermeneutica-c1-2026-07-09-v2]
    paths:
      - '.metadata/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate DataCite 4.4
        run: echo "Validating DataCite..."
      - name: Validate Dublin Core
        run: echo "Validating Dublin Core..."
      - name: Validate CodeMeta 2.0
        run: echo "Validating CodeMeta..."